import { SupabaseFixtures } from './supabase-fixtures.model';

describe('SupabaseFixtures', () => {
  it('should create an instance', () => {
    expect(new SupabaseFixtures()).toBeTruthy();
  });
});
